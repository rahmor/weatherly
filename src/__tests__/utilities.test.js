import parseWeatherJSON from '../utilities/utilities';

/*
  I need to pass in a json object.  I also need to mock Math.floor and parseUnixTime
*/

describe('utilites', () => {
  it('return an object', () => {});
});
